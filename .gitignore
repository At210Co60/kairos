# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright MCP 本地测试产物
.playwright-mcp/

# 本地检查截图与下载存档(不入库)
# 仅限仓库根目录的截图；src/assets 下的同名资源属于项目资源，必须入库
/kairos-*.png
lhm.zip

# 本地调试脚本与诊断输出(不入库)
/*.ps1
/fan-diag*.json
/fan-final.json
/repro-task.cjs
/task-diag*.txt
/uac-debug.txt
/task-reg-debug.txt
/whoami-elev.txt
/liquid-glass-demo.html

# LibreHardwareMonitor 的运行时状态：整个文件都是逐传感器的 plot 开关与采样历史
# (含本机网卡 GUID、电池数据)以及 authenticationPassword 哈希，每次运行都会被 LHM
# 重写，属于机器本地状态，不入库。缺失时 LHM 首次运行会自动生成默认配置。
electron/vendor/LibreHardwareMonitor/LibreHardwareMonitor.config

# 内核驱动同理：LHM 运行时从内嵌资源解出来写到这里的，不是发行文件
# (exe/dll 是 9/5 入库、构建日期 2024/11/24，这个 .sys 的时间戳是每次运行那一刻)
electron/vendor/LibreHardwareMonitor/LibreHardwareMonitor.sys

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
